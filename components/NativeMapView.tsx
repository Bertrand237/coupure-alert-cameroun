import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import Colors from '@/constants/colors';

export default function NativeMapView(_props: any) {
  return null;
}

export const isNativeMapAvailable = false;
